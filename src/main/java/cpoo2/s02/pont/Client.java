package cpoo2.s02.pont;

/**
 * S03 — exercice 4. Pont, puis Fabrique abstraite.
 *
 * <p>Rien n'est fourni ici : c'est un exercice de <b>conception</b>. À vous de
 * créer les paquets {@code cpoo2.s02.pont.api} et {@code cpoo2.s02.pont.impl}.
 *
 * <p>Q8 — l'API : {@code MutableList} et {@code ImmutableList}, chacune sachant
 * rendre l'élément à une position donnée et ajouter un élément. Pensez à
 * l'héritage. Une seule implémentation.
 *
 * <p>Q9 — ⚠ vous allez buter sur {@code ajouter}. Peut-elle vivre sur le parent
 * commun ? Écrivez votre réponse en commentaire ici même.
 *
 * <p>Q10 — la Fabrique abstraite : après elle, ce fichier ne doit plus contenir
 * <b>aucun {@code new}</b> de classe concrète.
 *
 * <p>Q11 — le code client ci-dessous est votre livrable.
 */
public final class Client {

	private Client() {
		super();
	}

	public static void main(final String[] args) {
		// TODO Q11
	}
}
