package cpoo2.s02.poidsmouche;

/**
 * S03 — exercice 6. Deux méthodes à écrire. Elles se ressemblent, mais une
 * seule des deux est un <b>poids-mouche</b> : à vous de dire laquelle, et
 * pourquoi l'autre ne peut pas l'être.
 */
public final class Collections {

	private Collections() {
		super();
	}

	/**
	 * TODO — renvoie une liste immuable ne contenant que l'objet donné.
	 *
	 * @param <T> le type des objets de la liste
	 * @param elt le seul objet à stocker dans la liste renvoyée
	 * @return une liste immuable ne contenant que l'objet donné
	 */
	public static <T> List<T> singletonList(final T elt) {
		return null;
	}

	/**
	 * TODO — renvoie la liste immuable vide.
	 *
	 * <p>Regardez la signature du test qui l'accompagne avant d'écrire : il ne
	 * demande pas seulement une liste vide.
	 */
	public static <T> List<T> emptyList() {
		return null;
	}
}
